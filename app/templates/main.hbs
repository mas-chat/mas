{{#each window in sortedWindows}}
    {{startWindowRow window sortedWindows}}
    {{#unless window.hidden}}
        {{render "window" window}}
    {{/unless}}
    {{endWindowRow window sortedWindows}}
{{/each}}
