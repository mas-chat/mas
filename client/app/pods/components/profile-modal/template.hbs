{{#modal-dialog title="Profile" terminateButton="Destroy account…" proceedButton="Update" cancel="closeModal" submit="edit" terminate="terminate"}}
    <span class="error-text">{{errorMsg}}</span>
    <form role="form">
        <div class="form-group">
            <label>Your name</label>
            {{input class="form-control" value=name}}
        </div>
        <div class="form-group">
            <label>Your email address</label>
            {{input class="form-control" value=email}}
        </div>
        <div class="form-group">
            <label>Your nick</label>
            {{input class="form-control" value=nick disabled="true"}}
        </div>
    </form>
    To set up your avatar, visit <a style="color:#cccccc" href="http://en.gravatar.com/" target="_blank">http://en.gravatar.com/</a>. Remember to use the same email address there as you use here in MAS.
{{/modal-dialog}}
