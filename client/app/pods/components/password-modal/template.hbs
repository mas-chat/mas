{{#modal-dialog title=passwordTitle proceedButton="Change" cancel="closeModal" submit="changePassword"}}
    <span class="error-text">{{errorMsg}}</span>
    <form role="form">
        <div class="new-row">
            <label>
                {{input type="checkbox" checked=modalPasswordEnabled}} Password enabled.
            </label>
        </div>
        <div class="form-group">
            <label for="password">Password</label>
            {{input class="form-control" elementId="password" value=modalPassword disabled=modalPasswordDisabled}}
        </div>
    </form>
{{/modal-dialog}}
