{{#if stores.windows.initDone}}
    {{modal-manager}}
    {{alert-message}}

    <div class="flex-grow-row flex-1">
        {{main-sidebar draggedWindow=draggedWindow}}
        {{window-grid draggedWindow=draggedWindow}}
    </div>

    {{window-context-menu}}
    {{unconfirmed-email}}
{{else}}
    <div class="flex-grow-row flex-1 flex-align-center">
        <div class="loader loader-big"></div>
    </div>
{{/if}}
