{{#each desktops key="id" as |desktop|}}
    {{desktop-button id=desktop.id name=desktop.initials selected=activeDesktop messages=desktop.messages}}
{{/each}}

{{#if activeDraggedWindow}}
    {{desktop-button id="new" name="+" selected=false messages=0}}
{{/if}}
