{{#each desktops key="id" as |desktop|}}
    {{desktop-button draggedWindow=draggedWindow id=desktop.id name=desktop.initials selected=activeDesktop messages=desktop.messages}}
{{/each}}

{{#if draggedWindow}}
    {{desktop-button draggedWindow=draggedWindow id="new" name="+" selected=false messages=0}}
{{/if}}
