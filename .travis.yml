language: node_js
node_js:
    - "7"
addons:
  sauce_connect: true
services:
  - docker
sudo: required
cache:
  directories:
  - $HOME/.yarn-cache
install:
  - curl -o- -L https://yarnpkg.com/install.sh | bash
  - PATH="$HOME/.yarn/bin:$PATH"
  - cd server && yarn && cd ..
  - cd server/website && yarn && cd ../..
  - cd newclient && yarn && cd ..
  - cd server/test/browser && yarn && cd ../../..
script:
  - cd server && npm test
